package org.la.core.java.student.wang.assignment9.Task12;

public class Child extends Parent{

   // Can we override a static method? No, we cannot override static methods because method overriding
    // is based on dynamic binding at runtime and the static methods are bonded using static binding at
    // compile time.
}
